---
search:
  exclude: true
---

# Selector 类

`Selector` 类是 Scrapling 的核心解析引擎，提供 HTML 解析与元素选择能力。

以下为 `Selector` 类的参数、属性与方法参考信息。

可直接从 `scrapling` 导入 `Selector` 类：

```python
from scrapling.parser import Selector
```

## ::: scrapling.parser.Selector
    handler: python
    :docstring:

## ::: scrapling.parser.Selectors
    handler: python
    :docstring:

